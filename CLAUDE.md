# Project Rules

See `.claude/rules/backend/` for all backend project rules.
See `.claude/rules/frontend/` for all frontend project rules.

Always use the `code-reviewer` subagent to review any code changes.
In case of any research is needed, use the `researcher` subagent.
